# Application Port
PORT=3000

# Session Secret (used by express-session)
SESSION_SECRET=replace_this_with_a_long_random_string_for_session

# JWT Secret (used to sign API tokens)
JWT_SECRET=replace_this_with_a_long_random_string_for_jwt
JWT_EXPIRES_IN=1h

# Node Environment
NODE_ENV=development
